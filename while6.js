/* faca um programa que pergunte ao usuario:
Insira zero para sair, ou qualquer outra tecla para continuar"
Se o usuario inserir zero ele sai, se nao o programa reinicia
novamente*/
var rs= require("readline-sync")

while (answer!=0){
 
 var answer =rs.question("Enter zero to exit or any other key to continue:")
  
}